import { Container } from 'react-bootstrap'

const Home = () => {
    return (
        <Container className="mt-5 text-center">
                <h2>We oooutie 5000 after we complete these tasks</h2>
        </Container>
    );
};

export default Home;